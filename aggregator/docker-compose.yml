services:
  uazapi-aggregator:
    build: .
    container_name: uazapi-aggregator
    restart: unless-stopped
    # host networking: alcanca os outros containers via localhost:<porta-publicada>
    network_mode: host
    environment:
      - PORT=4000
      - WEBHOOK_SECRET=${WEBHOOK_SECRET}
